<nav class='navbar navbar-default'>
  <div class='container'>
    <div class='navbar-brand'>
      {{#link-to 'index'}}ember-auth-demo{{/link-to}}
    </div>
    {{#if session.isAuthenticated}}
      <button class='btn btn-default navbar-btn' {{action 'signOut'}}>{{fa-icon 'sign-out'}} Sign Out</button>
    {{else}}
      {{#link-to 'sign-in'}}<button class='btn btn-default navbar-btn'>{{fa-icon 'sign-in'}} Sign In</button>{{/link-to}}
    {{/if}}
  </div>
</nav>